# Taxonomy Builder Backend

FastAPI backend for the Taxonomy Builder application.

## Development

```bash
# Install dependencies
uv sync --all-extras

# Run the development server
uv run uvicorn taxonomy_builder.main:app --reload

# Run tests
uv run pytest
```
