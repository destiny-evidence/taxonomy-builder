# Reverse Proxy for Multi-Worktree Development (localhost mode)
#
# Simple HTTP proxy for external contributors.
# No DNS setup required - just use localhost:PORT directly.
#
# For internal fef.dev setup with HTTPS, use:
#   docker compose -f docker-compose.yml -f docker-compose.fef.yml up
#
# Reload after changes: docker compose exec proxy caddy reload -c /etc/caddy/Caddyfile

# Import all worktree-specific configs
import sites/*

# Catch-all - routes to main worktree (default ports)
http://:80 {
	reverse_proxy /api/* host.docker.internal:8000
	reverse_proxy host.docker.internal:3000
}
